apiVersion: apps/v1
kind: Deployment
metadata:
  name: chatfrontend
  labels:
    app: redis-chat-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis-chat-app
  template:
    metadata:
      labels:
        app: redis-chat-app
    spec:
      containers:
      - name: chatfrontend
        image: baylon44/redis-chat-app:chatfrontend
        ports:
        - containerPort: 3000
        env:
        - name: REDIS_HOST
          value: redis-service
        - name: REDIS_PORT
          value: "6379"
---