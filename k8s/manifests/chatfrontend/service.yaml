apiVersion: v1
kind: Service
metadata:
  name: chatfrontend-service
spec:
  selector:
    app: redis-chat-app
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP

--- 